<ul class="unstyled well users-list">
	<% @users.each do |user| %>
	<li>
		<%= image_tag user.avatar.url, :class => 'thumbnail pull-left' %>
		<span class="email"><%= user.email %></span>
	</li>
	<% end %>
</ul>